@model IEnumerable<FinalEksamen.BestyrelseRole>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="container-fluid panel">
    <h2>bestyrelseRole Liste</h2>

    <p>
        @Html.ActionLink("Opret Ny", "Create", "", new { @class = "MyLink" })
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MedlemRole)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MedlemRole)
                </td>
                <td>
                    @Html.ActionLink("Rediger", "Edit", new { id = item.Id }, new { @class = "MyLink" }) |
                    @*@Html.ActionLink("Detalje", "Details", new { id = item.Id }, new { @class = "MyLink" }) |*@
                    @Html.ActionLink("selt", "Delete", new { id = item.Id }, new { @class = "MyLink" })
                </td>
            </tr>
        }

    </table>
</div>